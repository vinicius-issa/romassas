import { firestore } from '../../services/firebase';
import React, { useEffect } from 'react';

function listOrders(props:any) {
    
    useEffect(()=>{
        //firestore.collection('orders').where()
    })
    
    return (
        <div>
            Hello Wordl
        </div>
    );
}

export default listOrders;